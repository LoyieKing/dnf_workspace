// Auto-generated stub from DWARF info
// Original source: src/gene.cpp
// Compiler: GNU C++ 4.1.2 20070115 (prerelease) (SUSE Linux)
// 函数体暂为空；仅保留签名、参数名与局部变量名。

#include "inc/gene.h"
#include "yunfeiyang/dev/branch/src/common/tencrypt_new/<built-in>"
#include <bits/types.h>
#include <libio.h>
#include <stddef.h>
#include <stdio.h>

// line 8
void CGene::SetGeneKey(unsigned char *key, bool benc, unsigned char *genekey, unsigned char *gene) {
    // local: int i;
    // local: unsigned int pos;
    // local: unsigned char kps;
    // local: unsigned int holdrand;
    // local: unsigned char bak[];
}

// line 50
void CGene::Gene(unsigned char *key, bool benc, unsigned char *buf, unsigned int buflen) {
    // local: unsigned char gene[];
    // local: unsigned char genekey[];
    // local: unsigned int i;
}

// line 76
void CGene::Init(void *pSys, void *pLocal) {
}

// line 81
void CGene::UnInit() {
}

