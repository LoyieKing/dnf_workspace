// Auto-generated stub from DWARF info
// Original source: src/amoeba.cpp
// Compiler: GNU C++ 4.1.2 20070115 (prerelease) (SUSE Linux)
// 函数体暂为空；仅保留签名、参数名与局部变量名。

#include "inc/amoeba.h"
#include "include/TenCrypt.h"
#include "yunfeiyang/dev/branch/src/common/tencrypt_new/<built-in>"
#include <bits/types.h>
#include <libio.h>
#include <stddef.h>
#include <stdio.h>

// line 9
void CAmoeba::amoKey(unsigned char *key, unsigned char *add) {
    // local: int i;
    // local: int l;
}

// line 50
void CAmoeba::amoEnCrypt(unsigned char *key, unsigned char *buf) {
    // local: unsigned char pcrn[];
    // local: unsigned char bak;
    // local: int j;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
}

// line 86
void CAmoeba::amoDeCrypt(unsigned char *key, unsigned char *buf) {
    // local: unsigned char pcrn[];
    // local: unsigned char bak;
    // local: int j;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
    // local: int n;
}

// line 188
void CAmoeba::UnInit() {
}

// line 149
void CAmoeba::Init(void *pSys, void *pLocal) {
    // local: unsigned char _amobbit[];
    // local: unsigned char _amobb[];
    // local: unsigned char _amoIP[];
}

// line 122
void CAmoeba::Amoeba(unsigned char *key, bool benc, unsigned char *buf, unsigned int buflen) {
    // local: unsigned char Add[];
    // local: unsigned int nLen;
    // local: ITsLocal *pLocal;
    // local: unsigned int i;
}

