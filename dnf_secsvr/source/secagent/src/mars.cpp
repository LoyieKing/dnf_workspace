// Auto-generated stub from DWARF info
// Original source: src/mars.cpp
// Compiler: GNU C++ 4.1.2 20070115 (prerelease) (SUSE Linux)
// 函数体暂为空；仅保留签名、参数名与局部变量名。

#include "inc/mars.h"
#include "include/TenCrypt.h"
#include "yunfeiyang/dev/branch/src/common/tencrypt_new/<built-in>"
#include <bits/types.h>
#include <libio.h>
#include <stddef.h>
#include <stdio.h>

// line 248
void CMars::mars_setkey(unsigned char *in_key, unsigned int key_bits) {
    // local: unsigned int i;
    // local: unsigned int j;
    // local: unsigned int m;
}

// line 290
void CMars::mars_enc(unsigned char *in_blk, unsigned char *out_blk) {
    // local: unsigned int a;
    // local: unsigned int b;
    // local: unsigned int c;
    // local: unsigned int d;
    // local: unsigned int l;
    // local: unsigned int m;
    // local: unsigned int r;
}

// line 324
void CMars::mars_dec(unsigned char *in_blk, unsigned char *out_blk) {
    // local: unsigned int a;
    // local: unsigned int b;
    // local: unsigned int c;
    // local: unsigned int d;
    // local: unsigned int l;
    // local: unsigned int m;
    // local: unsigned int r;
}

// line 381
void CMars::Init(void *pSys, void *pLocal) {
}

// line 386
void CMars::UnInit() {
}

// line 358
void CMars::Mars(unsigned char *key, bool benc, unsigned char *buf, unsigned int buflen) {
    // local: unsigned int nLen;
    // local: ITsLocal *pLocal;
    // local: unsigned int i;
}

