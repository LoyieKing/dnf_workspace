# include/ —— tencrypt_new 对外头（独立工程产物）

- 来源：`/data/yunfeiyang/dev/branch/src/common/tencrypt_new`（GCC 4.1.2）
- 内容：TenCrypt.h / TenHash.h（对外 API）
- 桩镜像：`../source/secagent/include/`（仅 secagent 使用顶层 include/；
  gunnersvr / zergsvr 的加密子集只用 `inc/`）
